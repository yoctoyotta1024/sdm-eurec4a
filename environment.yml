name: sdm_eurec4a_env312
channels:
  - defaults
dependencies:
  - python = 3.12
  - pip >= 23.3.2
  - pytest >= 7.4.4
  # Further dependencies
  - numpy >= 1.26.3
  - scipy >= 1.11.4
  - pandas >= 2.1.4
  - lmfit >= 1.2.2
  - matplotlib >= 3.8.2
  - cftime >= 1.6.3
  - cartopy >= 0.22.0
  - click >= 8.1.7
  - pre-commit >= 3.6.0
  - bottleneck >= 1.3.5
  - netcdf4 >= 1.6.5
  - dask >= 2023.12.1
  - xarray >= 2023.12.0
  # Further dependencies for the analysis
  - pip:
    - pyshp >= 2.3.1 # necessary for cartopy to work on HPC (https://github.com/SciTools/cartopy/issues/2284)
    - ipykernel >= 7.3.0
    - tqdm >= 4.68.4
    - dask_jobqueue >= 0.9.0
    - awkward >= 2.10.0
    - mpi4py >= 4.1.2
    - ruamel.yaml >= 0.19.1
    - zarr >= 3.3.0
